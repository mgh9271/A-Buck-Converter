\documentclass{standalone}
\usepackage{siunitx}
\usepackage[american,cuteinductors,smartlabels]{circuitikz}

\usetikzlibrary{calc}
\ctikzset{bipoles/thickness=1}
\ctikzset{bipoles/length=0.8cm}
\ctikzset{bipoles/vsourceam/height/.initial=.8}
\ctikzset{bipoles/vsourceam/width/.initial=.8}
\tikzstyle{every node}=[font=\small]
\tikzstyle{every path}=[line width=0.8pt,line cap=round,line join=round]

\begin{document}
		\begin{circuitikz}
			\draw
			(0,0)
			to [V, invert, v=$V_S$, i>=$i$] ++(0,2.5)
			to[short] ++(1,0) node[above]{} 
			to[open, o-o, l=$S1$] ++(0.65,0) coordinate (A)
			to[short] ++(0.5,0)
			to[L, l=$L$, v=$v_{L}$] ++(1.5,0)
			to[short] ++(0.5,0) coordinate (B)
			to[short] ++(1.5,0)
			to[generic, v=$~~V_o$] ++(0,-2.5)
			--(0,0)
			
			(A)                                         
			to[short] ++(-0.65, -0.1)
			(B)                                         
			to[C, l=$C$, v=$v_{C}$] ++(0,-2.5)
			
			;
		\end{circuitikz}
	\end{document}